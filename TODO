nddiff
    --ctx-struct <num>
    --stats
    Fix text diff header

ndpatch --path <path>
    patch specified parts

ndquery
    --ignore <path> # Ignore desired parts
    --replace # Replace loaded file by output (canonize JSON etc)
    Refactor to OO

ndproc
    --list-sources <rules>
    --ignore-rule <comma,separated,numbers,list>
    --sqweeze-blame # make blame as small as possible (get rid of intermediate data)
    tests -h -V noargs and so on for all modules
    'optional' opt to mods opts %)
    validate_rules method for all modules
    STDIN/STDOUT processing

NDProc::Module::Insert
    --method append|override|prepend|shift # define a way to add items to arrays (override by default)

NDProc::Module::Merge
    --match <path> # (combined with path) -- to implement fair pairwise merge (filter like "defined" used)

NDProc::Module::Pipe
