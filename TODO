* nddiff --ctx-struct <num>

? nddiff --blame
    sqweeze blame and diff provided struct with it

* ndpatch --path <path>
    patch specified parts

* ndquery --ignore <path>
    Ignore desired parts

* ndquery --replace
    Replace loaded file by output (canonize JSON etc)

* ndquery: refactor to OO

* ndproc
    --builtin-rules <path>
    --list-sources <rules>
    --embed-rules <path>
    --embed-blame <path>
    --ignore-rule <comma,separated,numbers,list>
    tests -h -V noargs and so on for all modules
    'optional' opt to mods opts %)
    add method validate_opts to all modules

* NDProc::Module::Insert
    --shift # disabled by default (shift list's tails when inserting new values)

* NDProc::Module::Merge

* NDProc::Module::Pipe
