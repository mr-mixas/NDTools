* nddiff --ctx-struct <num>

? nddiff --blame
    sqweeze blame and diff provided struct with it

* ndpatch --path <path>
    patch specified parts

* ndquery --ignore <path>
    Ignore desired parts

* ndquery --replace
    Replace loaded file by output (canonize JSON etc)

* ndquery --strict
    Exit with 8 if specified path doesn't exists

* ndquery: refactor to OO

* ndproc
    --list-sources <rules>
    --embed-blame <path>
    --ignore-rule <comma,separated,numbers,list>
    tests -h -V noargs and so on for all modules
    'optional' opt to mods opts %)
    validate_rules method for all modules
    STDIN/STDOUT processing

* NDProc::Module::Insert
    --method append|prepend|override|shift # define a way to add items to arrays (override by default)

* NDProc::Module::Merge
    --filter <path> # (combined with path) -- to implement fair pairwise merge (filter like "defined" used)
    --pairwise <bool> # see above

* NDProc::Module::Pipe
