* nddiff --ctx-text <num>

* nddiff --ctx-struct <num>

* nddiff --patch
    1. Visualize patch if one argument used
    2. Make small possible diff (patch) when two arguments used

* ndpatch --path <path>
    patch specified parts

* ndproc
    Processing tool

* ndquery --ignore <path>
    Ignore desired parts

* NDProc::Module::Insert
    --path
    --override # disabled by default (item in arrays inserts shifting tail)
    --value <value> # insert structure specified by arg if omitted
